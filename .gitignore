# Go build output
/bin/
/dist/
/intent
*.test
*.out

# Installed skill files are generated by `intent install-skill`, never committed
# (DESIGN §12).
.claude/skills/intent/

# Local agent tooling — not part of the project.
.beads/
.claude/
.cursor/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
